<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Shoplify 0" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

  <body>
    <%= link_to "Products", products_path %>
    <hr>
    <%= yield %>
  </body>
</html>
